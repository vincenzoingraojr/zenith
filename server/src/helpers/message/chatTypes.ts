export const CHAT_TYPES = Object.freeze({
    CHAT: "chat",
    GROUP: "group",
    CHANNEL: "channel",
});

export const MESSAGE_TYPES = Object.freeze({
    MESSAGE: "message",
    EVENT: "event",
    FILE: "file",
    AUDIO: "audio",
    VIDEO: "video",
    IMAGE: "image",
    LOCATION: "location",
});

export const CHAT_USER_ROLES = Object.freeze({
    ADMIN: "admin",
    MEMBER: "member",
});

export const EVENT_TYPES = Object.freeze({
    CHAT: "chat",
    MESSAGE: "message",
});
