mutation addLandingUser(
    $name: String!
    $username: String!
    $email: String!
) {
    addLandingUser(
        name: $name
        username: $username
        email: $email
    ) {
        errors {
            field
            message
        }
        status
        ok
    }
}
