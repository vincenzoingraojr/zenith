mutation CreateRepost(
    $postId: String!
) {
    createRepost(
        postId: $postId
    ) {
        id
        repostId
        postId
        authorId
        createdAt
        updatedAt
    }
}