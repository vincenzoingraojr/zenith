query IsRepostedByUser(
    $postId: Int
    $userId: Int
) {
    isRepostedByUser(
        postId: $postId
        userId: $userId
    ) {
        id
        repostId
        postId
        authorId
        createdAt
        updatedAt
    }
}