query IsPostLikedByMe(
    $itemId: String!,
    $type: String!
) {
    isPostLikedByMe(
        itemId: $itemId,
        type: $type,
    ) {
        id
        userId
        likedItemId
        itemOpened
        itemType
        origin
        createdAt
        updatedAt
    }
}